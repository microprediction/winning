## R CMD check --as-cran results

0 errors, 0 warnings. NOTEs: new submission; local HTML-tidy/V8
tooling notes only.

Checked on R release (macOS). No reverse dependencies (new package).

## Submission log

- 2026-08-28: 0.1.0 uploaded to CRAN (new submission); upstream courtesy
  note sent to the mvtnorm maintainer the same day.
